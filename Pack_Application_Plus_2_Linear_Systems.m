# Finding Influence of Environment on Cyanobacteria Growth## All environmental variables are in A## b = relative cyanobacteria abundance## x = regression coefficientsdata = csvread("community_data.csv");data = data(2:length(data),2:19);# Variables for A matrix isolated and normalizedA = data(:,3:10);# Cyanobacteria relative abundance isolated and normalizedb = data(:,12);# Solving for coefficients of regression after normalizingA = zscore(A);b = zscore(b);x = A \ b;